<view class="choose_unit">
    <view class="list">
        <block wx:for="{{unitList}}" wx:key="item.index">
            <van-cell title="{{item.tenderName}}" is-link bind:tap="educationList" data-orgId="{{item.orgId}}" data-id="{{item.id}}" />
        </block>
    </view>
</view>