<van-collapse value="{{ activeNames }}" bind:change="onChange">
    <block wx:for="{{arr}}" wx:key="index">
        <van-collapse-item title="有赞微商城" :name="index">
            <view class="list">
                <block wx:for="{{arr}}" wx:key="index">
                    <view class="item">（010001）班组停工整改后，未履行复查程序，强行恢复施工【10分/次】</view>
                </block>
            </view>
        </van-collapse-item>
    </block>
</van-collapse>