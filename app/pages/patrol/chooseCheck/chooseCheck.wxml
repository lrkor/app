<view class="choose_unit">
    <view class="list">
        <block wx:for="{{unitList}}" wx:key="item.index">
            <van-cell title="{{item.name}}" is-link bindtap="checkList" data-id="{{item.id}}" />
        </block>
    </view>
</view>